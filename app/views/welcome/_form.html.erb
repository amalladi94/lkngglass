<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class=" section-heading">
    <%= f.label "Join the future!" %><br>
    <h3 class="section-subheading2 text-muted2">Put your email in below to get all the latest news about Looking Glass.</h3>
    <br>

    <%= f.text_field :email %>
    <br>
    <br>
  </div>
  
  
  <div class="actions">
    <%= f.submit "Submit " %>
  </div>
<% end %>
